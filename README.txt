Snippet	Description
-------	--------------------------------------------------
adc	ADD column
adcst	ADD CONSTRAINT
alc	ALTER COLUMN
alt	ALTER TABLE
bak	Ola Hallengren Backup
be	BEGIN...END block
bt	BEGIN TRANSACTION
cci	CREATE CLUSTERED INDEX
cfn	CREATE (scalar/inline) FUNCTION
cfnt	CREATE (table) FUNCTION
cj	CROSS JOIN
cni	CREATE NONCLUSTERED INDEX
cs	CREATE SYNONYM
csl	Comma Seperated List (STUFF)
csp	CREATE STORED PROCEDURE
ct	COMMIT TRAN
ctb	CREATE TABLE
cte	WITH CTE
cvw	CREATE VIEW
cw	CASE WHEN
dc	DROP COLUMN
dcst	DROP CONSTRAINT
defrg	Ola Hallengren Index Maintenance
df	DELETE FROM
dfn	DROP FUNCTION
dfw	DELETE FROM WHERE
di	DROP INDEX
dp	DROP PROCEDURE
ds	DROP SYNONYM
dt	DROP TABLE
dtr	DISABLE TRIGGER
dtra	DISABLE all TRIGGERs on table
dv	DROP VIEW
etr	ENABLE TRIGGER
etra	ENABLE all TRIGGERs on table
fkt	Foreign Keys for Table
foj	FULL OUTER JOIN
gb	GROUP BY 
header	Template header for db objects
ifs	IF w/ BEGIN..END
ii	INSERT INTO
ij	INNER JOIN
inn	IS NOT NULL
lk	LIKE '% %'
loj	LEFT OUTER JOIN
ls	DBCC SQLPERF for LOGSPACE
ob	ORDER BY
prr	Post-release recompile
roj	RIGHT OUTER JOIN
rt	ROLLBACK TRAN
sc	SYSCOMMENTS lookup
scnt	SELECT COUNT(*)
scntf	SELECT COUNT(*) nicely formatted
sf	SELECT FROM
sfv	SELECT FROM a value list
sfw	SELECT FROM WHERE
spb	sp_Blitz: Overall Health Check
sph	EXECUTE sp_help
spt	EXECUTE sp_helptext
spw	EXECUTE sp_whoisactive
ss	Set STATISTICS IO and STATISTICS TIME on and off
st10	SELECT TOP 10 rows
tc	TRY..CATCH block
today	CAST(GETDATE() AS DATE)
us	USE db
uv	User Validation block
xa	SET XACT_ABORT ON
xml	XML block

Ctrl + F12 goes to object in Object Explorer
